import styled from "styled-components";

const ButtonsGroupe = styled.div`
  display: flex;
  justify-content: space-between;
  gap: 5px;
  & button {
    flex: 1;
  }
`;

export default ButtonsGroupe;
